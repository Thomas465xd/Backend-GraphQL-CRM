const texts = [
    
]

// Define resolvers
const resolvers = {
	Query: {
		getText: (_, { input }, ctx ) => {
            return
        }, 
        getImage: () => {
            return
        }
	},
};

export default resolvers